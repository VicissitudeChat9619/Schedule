# 复制此文件为 application-local.yml 并填入真实的数据库密码等信息
# 运行时使用: java -jar myschedule-server.jar --spring.profiles.active=local
spring:
  datasource:
    url: jdbc:mysql://你的服务器IP:3306/schedule?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: root
    password: 你的MySQL密码

napcat:
  api-url: http://localhost:3000
